<%= render 'shared/pagetitle', pageTitle: "Recipes with #{@ingredient.name.capitalize}" %>
  <div class="row">
    <div class="col-md-4 col-md-offset-4 center">
    <%# <%= gravatar_for(@chef,size:150) %>
    </div>
</div>
</div>
<% if @ingredient.recipes.count > 0 %>
    <div class="container">
      <div align="center">
       <%= will_paginate @in_recipes %>
      </div>
      <%= render 'recipes/listings', obj: @in_recipes %>
    </div>
    <div align="center">
      <%= will_paginate @in_recipes %>
    </div>
  <% else %>
    <div align="center">
      <h4>Ingredient is not used in recipes so far<h4>
    </div>
 <% end %>
